# Exemplo de configuração para desenvolvimento local
# Copie este arquivo para `.env.local` e ajuste se necessário.

# API do backend (Node/Express) — apontar para seu backend local em dev
VITE_API_URL=http://localhost:3000/

# NOTA: variáveis que começam com VITE_ são expostas ao código cliente.
# Nunca adicione segredos aqui.
